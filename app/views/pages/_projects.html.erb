<!-- Project Filters -->
<div class="container mx-auto px-4">
  <div class="flex flex-wrap justify-center gap-4 mb-12">
    <button class="filter-btn active px-6 py-2 rounded-full bg-primary text-white hover:bg-primary/90 transition-colors" data-filter="all">
      All Projects
    </button>
    <button class="filter-btn px-6 py-2 rounded-full bg-white/10 text-white hover:bg-white/20 transition-colors" data-filter="web">
      Web Apps
    </button>
    <button class="filter-btn px-6 py-2 rounded-full bg-white/10 text-white hover:bg-white/20 transition-colors" data-filter="mobile">
      Mobile Apps
    </button>
    <button class="filter-btn px-6 py-2 rounded-full bg-white/10 text-white hover:bg-white/20 transition-colors" data-filter="api">
      API Development
    </button>
  </div>
</div>

<!-- Projects Grid -->
<div class="container mx-auto px-4">
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <!-- Project 1 -->
    <div class="project-card" data-category="web">
      <div class="glass-card overflow-hidden group">
        <div class="relative overflow-hidden">
          <img src="https://via.placeholder.com/600x400" alt="E-commerce Platform" class="w-full h-64 object-cover transform group-hover:scale-110 transition-transform duration-500">
          <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end">
            <div class="p-6">
              <div class="flex gap-2 mb-4">
                <span class="px-3 py-1 bg-primary/20 text-primary text-sm rounded-full">Ruby on Rails</span>
                <span class="px-3 py-1 bg-primary/20 text-primary text-sm rounded-full">React</span>
              </div>
            </div>
          </div>
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-white mb-2">E-commerce Platform</h3>
          <p class="text-gray-400 mb-4">A full-featured e-commerce solution with real-time inventory management and payment processing.</p>
          <div class="flex gap-4">
            <a href="#" class="text-primary hover:text-accent transition-colors">View Project</a>
            <a href="#" class="text-primary hover:text-accent transition-colors">GitHub</a>
          </div>
        </div>
      </div>
    </div>

    <!-- Project 2 -->
    <div class="project-card" data-category="mobile">
      <div class="glass-card overflow-hidden group">
        <div class="relative overflow-hidden">
          <img src="https://via.placeholder.com/600x400" alt="Mobile Fitness App" class="w-full h-64 object-cover transform group-hover:scale-110 transition-transform duration-500">
          <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end">
            <div class="p-6">
              <div class="flex gap-2 mb-4">
                <span class="px-3 py-1 bg-primary/20 text-primary text-sm rounded-full">React Native</span>
                <span class="px-3 py-1 bg-primary/20 text-primary text-sm rounded-full">Node.js</span>
              </div>
            </div>
          </div>
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-white mb-2">Mobile Fitness App</h3>
          <p class="text-gray-400 mb-4">A cross-platform fitness tracking application with personalized workout plans and progress analytics.</p>
          <div class="flex gap-4">
            <a href="#" class="text-primary hover:text-accent transition-colors">View Project</a>
            <a href="#" class="text-primary hover:text-accent transition-colors">GitHub</a>
          </div>
        </div>
      </div>
    </div>

    <!-- Project 3 -->
    <div class="project-card" data-category="api">
      <div class="glass-card overflow-hidden group">
        <div class="relative overflow-hidden">
          <img src="https://via.placeholder.com/600x400" alt="RESTful API Service" class="w-full h-64 object-cover transform group-hover:scale-110 transition-transform duration-500">
          <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end">
            <div class="p-6">
              <div class="flex gap-2 mb-4">
                <span class="px-3 py-1 bg-primary/20 text-primary text-sm rounded-full">Ruby on Rails</span>
                <span class="px-3 py-1 bg-primary/20 text-primary text-sm rounded-full">PostgreSQL</span>
              </div>
            </div>
          </div>
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-white mb-2">RESTful API Service</h3>
          <p class="text-gray-400 mb-4">A scalable API service with comprehensive documentation and robust security features.</p>
          <div class="flex gap-4">
            <a href="#" class="text-primary hover:text-accent transition-colors">View Project</a>
            <a href="#" class="text-primary hover:text-accent transition-colors">GitHub</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const filterButtons = document.querySelectorAll('.filter-btn');
    const projectCards = document.querySelectorAll('.project-card');

    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        // Update active button
        filterButtons.forEach(btn => btn.classList.remove('active', 'bg-primary'));
        filterButtons.forEach(btn => btn.classList.add('bg-white/10'));
        button.classList.add('active', 'bg-primary');
        button.classList.remove('bg-white/10');

        const filter = button.getAttribute('data-filter');

        // Filter projects
        projectCards.forEach(card => {
          if (filter === 'all' || card.getAttribute('data-category') === filter) {
            card.style.display = 'block';
            card.classList.add('animate-fade-in');
          } else {
            card.style.display = 'none';
            card.classList.remove('animate-fade-in');
          }
        });
      });
    });
  });
</script>

<style>
  .project-card {
    animation: fadeIn 0.5s ease-out forwards;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style>
