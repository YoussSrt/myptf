<!-- Hero Section -->
<section id="home" class="w-full min-h-screen flex items-center justify-center relative overflow-hidden bg-black snap-section" data-aos="fade-up">
  <!-- Animated Background -->
  <div id="particles-js" class="absolute inset-0 pointer-events-none"></div>
  <div class="absolute inset-0 bg-gradient-to-br from-primary/20 to-accent/20 opacity-50"></div>

  <!-- Content -->
  <div class="w-full max-w-[2000px] mx-auto px-4 md:px-6 lg:px-8 relative z-10">
    <div class="text-center max-w-4xl mx-auto">
      <!-- Profile Image -->
      <div class="relative w-32 h-32 md:w-40 md:h-40 mx-auto mb-8 animate-float">
        <div class="absolute inset-0 rounded-full bg-gradient-to-r from-primary to-accent opacity-20 blur-lg"></div>
        <img src="https://avatars.githubusercontent.com/YoussSrt" alt="Youssef Saroute"
             class="w-full h-full rounded-full object-cover border-4 border-white/10">
      </div>

      <!-- Name & Title -->
      <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 bg-gradient-to-r from-primary to-accent bg-clip-text text-transparent"
          data-aos="fade-up">
        Youssef Saroute
      </h1>

      <!-- Dynamic Tagline -->
      <div class="text-xl md:text-2xl lg:text-3xl text-white/80 mb-8 font-light"
           data-aos="fade-up" data-aos-delay="200">
        <span id="typed-text"></span>
      </div>

      <!-- Brief Introduction -->
      <p class="text-lg md:text-xl text-white/70 mb-8 max-w-3xl mx-auto leading-relaxed"
         data-aos="fade-up" data-aos-delay="300">
        Bridging the gap between business strategy and technical innovation to drive sustainable impact.
        Specialized in transforming complex challenges into elegant, efficient solutions.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row justify-center items-center gap-4 sm:gap-6"
           data-aos="fade-up" data-aos-delay="400">
        <a href="#projects" class="btn-primary group relative overflow-hidden">
          <span class="relative z-10">Discover My Work</span>
          <div class="absolute inset-0 bg-gradient-to-r from-primary to-accent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        </a>
        <a href="#contact" class="btn-secondary group relative overflow-hidden">
          <span class="relative z-10">Let's Connect</span>
          <div class="absolute inset-0 bg-white/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        </a>
      </div>

      <!-- Quick Stats -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-16 max-w-3xl mx-auto" data-aos="fade-up" data-aos-delay="500">
        <div class="glass-card p-4 md:p-6 transform hover:scale-105 transition-all duration-300">
          <div class="text-2xl md:text-3xl font-bold text-primary mb-2 counter" data-target="8">0</div>
          <div class="text-sm md:text-base text-white/60">Years Experience</div>
        </div>
        <div class="glass-card p-4 md:p-6 transform hover:scale-105 transition-all duration-300">
          <div class="text-2xl md:text-3xl font-bold text-primary mb-2 counter" data-target="25">0</div>
          <div class="text-sm md:text-base text-white/60">Projects Delivered</div>
        </div>
        <div class="glass-card p-4 md:p-6 transform hover:scale-105 transition-all duration-300">
          <div class="text-2xl md:text-3xl font-bold text-primary mb-2 counter" data-target="15">0</div>
          <div class="text-sm md:text-base text-white/60">Happy Clients</div>
        </div>
        <div class="glass-card p-4 md:p-6 transform hover:scale-105 transition-all duration-300">
          <div class="text-2xl md:text-3xl font-bold text-primary mb-2 counter" data-target="3">0</div>
          <div class="text-sm md:text-base text-white/60">Impact Initiatives</div>
        </div>
      </div>

      <!-- Scroll Indicator -->
      <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
        <svg class="w-6 h-6 text-white/40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </div>
    </div>
  </div>
</section>

<!-- About Section -->
<section id="about" class="w-full min-h-screen relative overflow-hidden bg-[#001F23] bg-opacity-40 backdrop-blur-sm snap-section">
  <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
  <div class="w-full max-w-[2000px] mx-auto px-4 md:px-6 lg:px-8 py-20">
    <%= render 'pages/about' %>
  </div>
</section>

<!-- Projects Section -->
<section id="projects" class="w-full min-h-screen relative overflow-hidden bg-black snap-section">
  <div class="absolute inset-0 bg-dots-pattern opacity-5"></div>
  <div class="w-full max-w-[2000px] mx-auto px-4 md:px-6 lg:px-8 py-20">
    <%= render 'pages/projects' %>
  </div>
</section>

<!-- Skills Section -->
<section id="skills" class="w-full min-h-screen relative overflow-hidden bg-[#001F23] bg-opacity-40 backdrop-blur-sm snap-section">
  <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
  <div class="w-full max-w-[2000px] mx-auto px-4 md:px-6 lg:px-8 py-20">
    <%= render 'pages/skills' %>
  </div>
</section>

<!-- Contact Section -->
<section id="contact" class="w-full min-h-screen relative overflow-hidden bg-black snap-section">
  <div class="absolute inset-0 bg-dots-pattern opacity-5"></div>
  <div class="w-full max-w-[2000px] mx-auto px-4 md:px-6 lg:px-8 py-20">
    <%= render 'pages/contact' %>
  </div>
</section>

<style>
.bg-grid-pattern {
  background-image: linear-gradient(var(--primary-color) 1px, transparent 1px),
    linear-gradient(to right, var(--primary-color) 1px, transparent 1px);
  background-size: 50px 50px;
  width: 100vw;
  height: 100vh;
}

.bg-dots-pattern {
  background-image: radial-gradient(var(--primary-color) 1px, transparent 1px);
  background-size: 20px 20px;
  width: 100vw;
  height: 100vh;
}

@keyframes float {
  0% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
  100% { transform: translateY(0px); }
}

.animate-float {
  animation: float 3s ease-in-out infinite;
}

/* Snap Scrolling */
html {
  scroll-snap-type: y mandatory;
  scroll-behavior: smooth;
  overflow-x: hidden;
}

.snap-section {
  scroll-snap-align: start;
  scroll-snap-stop: always;
  display: flex;
  align-items: center;
  min-height: 100vh;
}

/* Enhanced Animations */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

[data-aos="fade-up"] {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease-out, transform 0.6s ease-out;
}

[data-aos="fade-up"].aos-animate {
  opacity: 1;
  transform: translateY(0);
}

/* Responsive Container */
@media (min-width: 2000px) {
  .max-w-[2000px] {
    max-width: 90vw;
  }
}

/* Glass Card Hover Effect */
.glass-card {
  @apply relative overflow-hidden transition-all duration-300;
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(10px);
}

.glass-card::before {
  content: '';
  @apply absolute inset-0 bg-gradient-to-r from-primary/5 to-accent/5 opacity-0 transition-opacity duration-300;
}

.glass-card:hover::before {
  @apply opacity-100;
}

/* Button Animations */
.btn-primary, .btn-secondary {
  @apply relative overflow-hidden transition-all duration-300 transform hover:scale-105;
}

.btn-primary::after, .btn-secondary::after {
  content: '';
  @apply absolute inset-0 bg-gradient-to-r from-primary to-accent opacity-0 transition-opacity duration-300;
}

.btn-primary:hover::after, .btn-secondary:hover::after {
  @apply opacity-10;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Initialize Typed.js with refined content
  const typed = new Typed('#typed-text', {
    strings: [
      'Strategy and Operations Consultant',
      'Full Stack Developer',
      'Impact and Tech Enthusiast',
      'Innovation Catalyst',
      'Data-Driven Solutions'
    ],
    typeSpeed: 50,
    backSpeed: 30,
    backDelay: 2000,
    loop: true
  });

  // Initialize Particles.js with optimized settings
  particlesJS('particles-js', {
    particles: {
      number: { value: 40, density: { enable: true, value_area: 800 } },
      color: { value: '#ffffff' },
      shape: { type: 'circle' },
      opacity: { value: 0.3, random: true },
      size: { value: 2, random: true },
      line_linked: {
        enable: true,
        distance: 150,
        color: '#ffffff',
        opacity: 0.2,
        width: 1
      },
      move: {
        enable: true,
        speed: 1,
        direction: 'none',
        random: true,
        straight: false,
        out_mode: 'out',
        bounce: false
      }
    },
    interactivity: {
      detect_on: 'canvas',
      events: {
        onhover: { enable: true, mode: 'repulse' },
        onclick: { enable: true, mode: 'push' },
        resize: true
      }
    },
    retina_detect: true
  });

  // Enhanced counter animation with IntersectionObserver
  const counters = document.querySelectorAll('.counter');

  const animateCounter = (counter, target) => {
    const duration = 2000;
    const steps = 50;
    const stepValue = target / steps;
    let current = 0;

    const updateCounter = () => {
      current += stepValue;
      if (current > target) current = target;
      counter.textContent = Math.round(current);

      if (current < target) {
        requestAnimationFrame(updateCounter);
      }
    };

    updateCounter();
  };

  const counterObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const counter = entry.target;
        const target = parseInt(counter.getAttribute('data-target'));
        animateCounter(counter, target);
        counterObserver.unobserve(counter);
      }
    });
  }, {
    threshold: 0.5
  });

  counters.forEach(counter => counterObserver.observe(counter));
});
</script>
